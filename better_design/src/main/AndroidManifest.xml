<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.betterdesigntwo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.BetterDesignSearchActivity"></activity>
        <activity
            android:name=".activity.BetterDesignDeepActivity"
            android:label="返回" />
        <activity android:name=".activity.BetterDesignActivity"></activity>
    </application>

</manifest>